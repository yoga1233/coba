import 'package:flutter/material.dart';

class TabNotification extends StatelessWidget {
  const TabNotification({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Ini Page Notification'),
    );
  }
}
